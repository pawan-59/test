name: Creating PR using Multi-Gitter

on:
  pull_request:
    types: [opened]
    branches:
      - develop
      - main

jobs:
  update-dependencies:
    runs-on: ubuntu-latest
    steps:
      - name: test
        run: |
          echo "Hello"
